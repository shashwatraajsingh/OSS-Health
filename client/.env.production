# Production API URL - Using Netlify proxy to Render backend
# This will use the proxy configured in netlify.toml
REACT_APP_API_URL=/api

# Alternative: Direct connection to Render (replace 'your-app-name')
# REACT_APP_API_URL=https://your-app-name.onrender.com/api
